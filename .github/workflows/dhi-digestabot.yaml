name: DHI Base Image Digestabot

on:
  workflow_dispatch:
  schedule:
    # Run weekly on Sundays at midnight UTC to check for DHI base image updates
    - cron: "0 0 * * 0"

permissions:
  contents: read

jobs:
  dhi-digestabot:
    name: DHI Base Image Digestabot
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write
      id-token: write

    steps:
    - name: Harden the runner (Audit all outbound calls)
      uses: step-security/harden-runner@f4a75cfd619ee5ce8d5b864b0d183aff3c69b55a # v2.13.1
      with:
        egress-policy: audit

    - name: Checkout repository
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

    # Run digestabot specifically for the DHI Dockerfile
    - name: Update DHI base image digests
      uses: chainguard-dev/digestabot@a3b776c1ca57d3127c85578cde8fef6eed3c75d3 # v1.2.3
      id: digestabot
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        include-files: "Dockerfile dhi"
        title-for-pr: 'Update DHI base image digests'
        commit-message: 'Update DHI base image digests'
        branch-for-pr: dhi-digestabot
        labels-for-pr: 'dependencies,security,dhi-images'

    - name: Summary
      if: steps.digestabot.outputs.pull_request_number
      run: |
        echo "## DHI Base Image Update" >> $GITHUB_STEP_SUMMARY
        echo "A pull request has been created to update the DHI base image digests." >> $GITHUB_STEP_SUMMARY
        echo "**Pull Request Number:** #${{ steps.digestabot.outputs.pull_request_number }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Images Currently Tracked:" >> $GITHUB_STEP_SUMMARY
        echo "- **Build Stage:** \`demonstrationorg/dhi-temurin:21-jdk-alpine3.21-dev@sha256:d32f08b3aa18668323a82e3ffb297cb1030dc1ed0d85b9786204538ab6e1a32a\`" >> $GITHUB_STEP_SUMMARY
        echo "- **Runtime Stage:** \`demonstrationorg/dhi-temurin:21_whale1@sha256:0189f624ac7166b288a2b127d30cb511b349d6cec5ecae5463051392d2a3a821\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Security Benefits:" >> $GITHUB_STEP_SUMMARY
        echo "- 🔒 Images pinned to specific digests for reproducible builds" >> $GITHUB_STEP_SUMMARY
        echo "- 🛡️ Automatic tracking of DHI security updates" >> $GITHUB_STEP_SUMMARY
        echo "- 📋 Complete audit trail of base image changes" >> $GITHUB_STEP_SUMMARY
        echo "- 🚀 Supply chain integrity through cryptographic verification" >> $GITHUB_STEP_SUMMARY
        echo "- ⬅️ Shift-left security with proactive vulnerability management" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### DHI Golden Images Benefits:" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Pre-hardened container foundation" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Security patches applied by experts" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Reduced attack surface compared to standard images" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Compliance with security best practices" >> $GITHUB_STEP_SUMMARY